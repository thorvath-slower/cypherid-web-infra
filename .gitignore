# Compiled files
*.tfstate
*.tfstate.*.backup
*.tfstate.backup
*tfvars

# Module directory
.terraform/

# Pycharm folder
.idea

# Editor Swap Files
*.swp
*.swo
*.swn
*.swm
*.swl
*.swk

/terraform.d/plugins
/terraform.d/modules

.DS_Store
.vscode
.envrc

# Scala language server
.metals

buildevents.plan
check-plan.output

venv

# CZID-198 (STATE-2): the Terraform dependency lock file MUST be committed (reproducible builds —
# CI installs from the lock, never re-resolves). Do NOT ignore it. Generate per stack with:
#   terraform init && terraform providers lock -platform=linux_amd64 -platform=darwin_arm64
# (.terraform.lock.hcl intentionally NOT ignored.)

# tf-linux plan files
tfplan.bin
